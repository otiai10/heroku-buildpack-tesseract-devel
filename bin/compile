#!/usr/bin/env bash
echo "-----> [Bla Bla Bla Bla]"

mkdir -p $1/.profile.d
ENVVARS=$1/.profile.d/build-env.sh

echo "export FOO=\"${1}\"" >> $ENVVARS

tar -zxvf tess-dir.tar.gz -C ${1}
echo "export TESSDATA_PREFIX=\"${1}/share/tesseract-ocr/\"" >> $ENVVARS
